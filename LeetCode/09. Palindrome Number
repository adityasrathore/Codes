class Solution {
public:
    bool isPalindrome(int x) {
        int num=x;
        long p=0;
        while(x>0){
            p=x%10+p*10;
            x=x/10;
        }
       if(p==num)
            return true;
        else 
            return false;
    }
};
